{
  "name": "resume-builder",
  "repository": {
    "type": "git",
    "url": "https://github.com/amdigbari/resume-builder"
  },
  "workspaces": [
    "resume_hub"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write",
    "format:all": "prettier --write '**/*.{js,ts,jsx,tsx,json,scss,css,mjs}'"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "prettier": "^3.3.2",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "turbo": "^2.0.5"
  },
  "packageManager": "yarn@4.1.1",
  "engines": {
    "node": ">=18",
    "yarn": ">=4"
  }
}
